version: "3"

services:    
    server-api:
        build: 
            context: src/server/
        ports:
            - "8000:80"
        restart: always
    client_react-web:
        build: 
            context: src/client/
        ports:
            - "8001:3000"
        restart: always
